Before writing or modifying any code, read `CODING_GUIDELINES.md` and follow all rules defined there.

# {{project.name}} ({{project.framework}})

All architectural rules, import conventions, verification checklists, and code style are documented in `CODING_GUIDELINES.md`. Do not duplicate those rules here.

## Quick Reference

- Framework: {{project.framework}}
- Language: {{project.language}}
- Main branch: `{{conventions.mainBranch}}`
{{#each verification.commands}}
- {{name}}: `{{command}}`
{{/each}}
{{#if hasCustomRules}}

## Project-Specific Notes

{{#each conventions.customRules}}
- {{this}}
{{/each}}
{{/if}}

{{#if analysisInsights}}
## Current Health: {{analysisInsights.healthGrade}} ({{analysisInsights.healthScore}}/100)

Top issues to avoid making worse:
{{#if analysisInsights.hasDeadExports}}
- **Dead exports** — Search for existing functions before creating new ones
{{/if}}
{{#if analysisInsights.hasDuplication}}
- **Code duplication** — Reuse existing logic, don't copy-paste
{{/if}}
{{#if analysisInsights.hasCircularDeps}}
- **{{analysisInsights.circularDeps}} circular dependency cycle(s)** — Do not add imports that create cycles
{{/if}}
{{#if analysisInsights.hasBarrelViolations}}
- **{{analysisInsights.barrelViolations}} barrel violations** — Import from barrel files, not internal files
{{/if}}
{{#if analysisInsights.hasLayerViolations}}
- **{{analysisInsights.layerViolations}} layer violations** — Lower layers must not import from higher layers
{{/if}}
{{#if analysisInsights.hasComplexity}}
- **High complexity files** — Keep branching logic minimal
{{/if}}

See `CODING_GUIDELINES.md` for full details on each issue.
{{/if}}

---
*Generated by [goodbot](https://github.com/timeritual/goodbot-ai)*
