# Coding Guidelines — {{project.name}}

> **For AI coding agents:** Read this entire file before writing or modifying any code.

## Architecture

{{#if hasLayers}}
### Module Layers (downward-only dependencies)

Higher layers may import from lower layers, never the reverse.

```
{{architecture.layerDiagramAscii}}
```

{{> import-rules}}

{{> business-logic-placement}}
{{else}}
This project uses a {{project.framework}} ({{project.language}}) stack.
{{/if}}

{{#if hasRedFlags}}
### Red Flags

If you see any of these patterns in code you're writing, stop and reconsider:

{{#each businessLogic.redFlags}}
- {{this}}
{{/each}}
{{/if}}

{{#if hasVerification}}
## Verification Checklist

Before committing, always run:

{{#each verification.commands}}
{{add @index 1}}. `{{command}}` — {{name}}
{{/each}}
{{/if}}

{{#if architecture.interfaceContracts}}
## Public Function Protocol

When adding a new public function to {{#each businessLogic.allowedIn}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}:

1. Add the function to the barrel `index.ts`
2. Add the signature to `interfaces.ts` with a `satisfies` check
3. Write or update tests
{{/if}}

## Code Style

- {{#if isTypescript}}TypeScript for all new files{{else}}{{project.language}} for all new files{{/if}}
- Follow existing linter rules strictly — zero warnings allowed
- Match the formatting and naming conventions of surrounding code
{{#if hasCustomRules}}

## Project-Specific Rules

{{#each conventions.customRules}}
- {{this}}
{{/each}}
{{/if}}

## Git Workflow

- Main branch: `{{conventions.mainBranch}}`
- Do not commit `.env`, credentials, or large binaries
- Run the verification checklist before every commit

---
*Generated by [goodbot](https://github.com/protocoding/goodbot-ai)*
